<%= form_with(model: exp_type, local: true) do |form| %>
  <% if exp_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exp_type.errors.count, "error") %> prohibited this exp_type from being saved:</h2>

      <ul>
      <% exp_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
